# ~/.bashrc: executed by bash(1) for non-login shells.
# see /usr/share/doc/bash/examples/startup-files (in the package bash-doc)
# for examples

# If not running interactively, don't do anything
case $- in
    *i*) ;;
      *) return;;
esac

# don't put duplicate lines or lines starting with space in the history.
# See bash(1) for more options
HISTCONTROL=ignoreboth

# append to the history file, don't overwrite it
shopt -s histappend

# for setting history length see HISTSIZE and HISTFILESIZE in bash(1)
HISTSIZE=1000
HISTFILESIZE=2000

# check the window size after each command and, if necessary,
# update the values of LINES and COLUMNS.
shopt -s checkwinsize

# If set, the pattern "**" used in a pathname expansion context will
# match all files and zero or more directories and subdirectories.
#shopt -s globstar

# make less more friendly for non-text input files, see lesspipe(1)
[ -x /usr/bin/lesspipe ] && eval "$(SHELL=/bin/sh lesspipe)"

# set variable identifying the chroot you work in (used in the prompt below)
if [ -z "${debian_chroot:-}" ] && [ -r /etc/debian_chroot ]; then
    debian_chroot=$(cat /etc/debian_chroot)
fi

# set a fancy prompt (non-color, unless we know we "want" color)
case "$TERM" in
    xterm-color) color_prompt=yes;;
esac

# uncomment for a colored prompt, if the terminal has the capability; turned
# off by default to not distract the user: the focus in a terminal window
# should be on the output of commands, not on the prompt
force_color_prompt=yes

if [ -n "$force_color_prompt" ]; then
    if [ -x /usr/bin/tput ] && tput setaf 1 >&/dev/null; then
	# We have color support; assume it's compliant with Ecma-48
	# (ISO/IEC-6429). (Lack of such support is extremely rare, and such
	# a case would tend to support setf rather than setaf.)
	color_prompt=yes
    else
	color_prompt=
    fi
fi

if [ "$color_prompt" = yes ]; then
    PS1='${debian_chroot:+($debian_chroot)}\[\033[01;32m\]\u@\h\[\033[00m\]:\[\033[01;34m\]\w\[\033[00m\]\$ '
else
    PS1='${debian_chroot:+($debian_chroot)}\u@\h:\w\$ '
fi
unset color_prompt force_color_prompt

# If this is an xterm set the title to user@host:dir
case "$TERM" in
xterm*|rxvt*)
    PS1="\[\e]0;${debian_chroot:+($debian_chroot)}\u@\h: \w\a\]$PS1"
    ;;
*)
    ;;
esac

# enable color support of ls and also add handy aliases
if [ -x /usr/bin/dircolors ]; then
    test -r ~/.dircolors && eval "$(dircolors -b ~/.dircolors)" || eval "$(dircolors -b)"
    alias ls='ls --color=auto'
    #alias dir='dir --color=auto'
    #alias vdir='vdir --color=auto'

    alias grep='grep --color=auto'
    alias fgrep='fgrep --color=auto'
    alias egrep='egrep --color=auto'
fi

# some more ls aliases
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'

# Add an "alert" alias for long running commands.  Use like so:
#   sleep 10; alert
alias alert='notify-send --urgency=low -i "$([ $? = 0 ] && echo terminal || echo error)" "$(history|tail -n1|sed -e '\''s/^\s*[0-9]\+\s*//;s/[;&|]\s*alert$//'\'')"'

# Alias definitions.
# You may want to put all your additions into a separate file like
# ~/.bash_aliases, instead of adding them here directly.
# See /usr/share/doc/bash-doc/examples in the bash-doc package.

if [ -f ~/.bash_aliases ]; then
    . ~/.bash_aliases
fi

# enable programmable completion features (you don't need to enable
# this, if it's already enabled in /etc/bash.bashrc and /etc/profile
# sources /etc/bash.bashrc).
if ! shopt -oq posix; then
  if [ -f /usr/share/bash-completion/bash_completion ]; then
    . /usr/share/bash-completion/bash_completion
  elif [ -f /etc/bash_completion ]; then
    . /etc/bash_completion
  fi
fi

#git config from Udacity Course. 
#Requires git-completion.bash and git-prompt.sh
##############################
# Enable tab completion
source ~/.githelpers/git-completion.bash

# colors!
green="\[\033[0;32m\]"
blue="\[\033[0;34m\]"
purple="\[\033[0;35m\]"
reset="\[\033[0m\]"

# Change command prompt
source ~/.githelpers/git-prompt.sh
export GIT_PS1_SHOWDIRTYSTATE=1
# '\u' adds the name of the current user to the prompt
# '\$(__git_ps1)' adds git-related stuff
# '\W' adds the name of the current directory
#original line from Udacity for PS1 is commented. Next line defined by me.
#export PS1="$purple\u$green\$(__git_ps1)$blue \W $ $reset
export PS1="${debian_chroot:+($debian_chroot)}\[\033[01;32m\]\u@\h\[\033[00m\]:$purple\$(__git_ps1)\[\033[01;34m\]\w\[\033[00m\]\$ "
##############################

alias pycharm='/home/frederik/software/pycharm-community-2017.1.2/bin/pycharm.sh'
alias alsdc='. /home/frederik/Documents/lsdc/lsdcenv/bin/activate'
alias alsdc12='. /home/frederik/Documents/lsdc/lsdcenv_r12/bin/activate'

export MUJOCO_PY_MJKEY_PATH=/home/frederik/Documents/lsdc/src/3rdparty/mjpro131/mjkey.txt
export MUJOCO_PY_MJPRO_PATH=/home/frederik/Documents/lsdc/src/3rdparty/mjpro131

#export MUJOCO_PY_MJKEY_PATH=/home/frederik/Documents/mujoco141/mjpro140/mjkey.txt
#export MUJOCO_PY_MJPRO_PATH=/home/frederik/Documents/mujoco141/mjpro140

export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/home/frederik/Documents/lsdc/build/lib
export PYTHONPATH=$PYTHONPATH:/home/frederik/Documents/lsdc/build/lib
export PYTHONPATH=$PYTHONPATH:/home/frederik/Documents/lsdc/python

export PATH=$PATH:/home/frederik/software/mendeleydesktop-1.17-linux-x86_64/bin
export LD_LIBRARY_PATH="$LD_LIBRARY_PATH:/usr/local/cuda/lib64:/usr/local/cuda-7.5/extras/CUPTI/lib64"
export CUDA_HOME=/usr/local/cuda-7.5

export PATH=/home/frederik/software/Qt/5.7/gcc_64/bin:$PATH

export PYTHONPATH=$PYTHONPATH:/home/frederik/Documents/gps/build/lib
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/home/frederik/Documents/gps/build/lib

alias fwd_jupyter='ssh -N -f -L localhost:8888:localhost:8889 febert@newton2'
alias fwd_tensorboard_newton2='ssh -N -f -L localhost:8888:localhost:8890 febert@newton2'
alias fwd_tensorboard_newton1='ssh -N -f -L localhost:8890:localhost:8890 febert@newton1'

export IBUS_ENABLE_SYNC_MODE=1

export VISUAL=vim
export EDITOR="$VISUAL"

alias arobo='. /home/frederik/Documents/RoboGym/robogym_env/bin/activate'

source /opt/ros/indigo/setup.bash

#activate lsdc environment:
#source /home/frederik/Documents/lsdc/lsdcenv/bin/activate

export PATH=$PATH:/usr/local/cuda/bin

export PATH=/home/frederik/software/anaconda2/bin:$PATH


#export PYTHONPATH=$PYTHONPATH:/home/frederik/Documents/rcnn/Faster-RCNN_TF/lib
export PYTHONPATH=$PYTHONPATH:/home/frederik/Documents/rcnn/FastMaskRCNN/libs

alias mrcnn='. /home/frederik/Documents/rcnn/FastMaskRCNN/mrcnn/bin/activate'
